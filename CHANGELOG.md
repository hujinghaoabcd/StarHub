# 更新日志 / Changelog

本文件记录用户可感知的重要变化。版本尚未正式发布的内容位于 `Unreleased`；实现状态和剩余风险以[项目状态](docs/development/PROJECT_STATUS.md)为准。

## [Unreleased] - 2026-08-04

### Added / 新增

- 独立“重点标记”功能：可标记重要仓库、筛选并优先排序，不创建分类关系。
- 仅分类名称导入：支持文本、CSV、JSON 数组、对象数组和 StarHub 备份中的名称提取，不导入项目关系。
- 正式分类注册表：稳定 category ID、中英文名称、别名、说明、示例、排除项、层级和 schema 版本。
- 分类治理预览：显示新增、更新、重命名、合并和冲突；阻塞冲突解决前不能应用。
- 分类安全迁移：重命名保持 ID，合并完整保留关系并去重，迁移前快照，支持撤销。
- 分类管理搜索、按项目数排序和空分类筛选。
- 持久化 AI 分类任务：分段、进度、暂停、恢复、取消、失败项重试和跨刷新恢复。
- AI 审核队列：分类、置信度、理由、人工选择、质量评价、确认写入和撤销。
- README 二阶段增强：只处理低置信度或人工标错候选，包含缓存、输入截取和前后结果审核。
- GitHub Pages 部署提交标识与公网冒烟验证。

### Changed / 变更

- 仓库详情合并为单一摘要卡：名称、描述、GitHub、About、Pages、取消 Star、元数据和重点按钮协调布局，README 与分类编辑位于其下。
- AI 分类改为只使用用户确认的正式注册表，模型不再自由创建分类名称。
- AI 第一轮默认只使用仓库元数据，不再预先抓取全部 README。
- API Key 从长期 localStorage 改为 sessionStorage，并提供一键清除；非敏感连接偏好继续本地保存。
- 自定义 AI API 地址要求公网 HTTPS，禁止凭据、query、fragment、localhost 与私网地址。
- AI 输出采用供应商结构化输出或 JSON 模式，并校验 Schema、批次短 ID、遗漏、重复、未知仓库与未知分类。
- 暂停状态允许确认写入当前有效草稿；写入后结束任务，剩余仓库由新任务继续。
- IndexedDB 升级至 v8，StarHub 完整备份升级至 v4，正式分类元数据升级至 schema v2。
- 分页支持 50、100、200、500、1000，筛选后对完整结果排序再分页。
- 更新分页、按钮、分类颜色和长名称显示，减少重复颜色和主题状态不一致。
- 登录页全部产品文案接入中英文语言包，删除未启用的 PWA、固定 18 分类、未评测性能数字和“自动写入”暗示。
- 全面重写中英文 README、用户指南、AI/数据配置、部署、故障排除、架构、开发状态和接手路线。

### Fixed / 修复

- 修复快速切换约十个仓库详情后页面卡顿或无响应：取消过期 README 请求、隔离 Markdown worker 渲染并避免旧结果覆盖新详情。
- 修复 AI JSON 截断后被自动补齐并误写的问题；无效输出现在拒绝并记录失败。
- 修复模型返回注册表外 category ID 仍继续任务的问题；无有效结果时自动暂停。
- 修复停止按钮不终止在途 GitHub/AI 请求、批次写入未等待和错误批次静默显示完成。
- 修复取消 Star 在后台同步期间长期不可用及旧同步快照回写。
- 修复分类合并时关系丢失、重命名产生新分类和侧栏排序/空分类管理困难。
- 修复详情操作按钮覆盖描述文字以及视觉风格不一致。
- 修复登录页仍宣称 PWA 离线可用、固定预设分类和未经基准测试的搜索延迟。

### Security / 安全

- GitHub OAuth 使用 state、PKCE S256、JSON POST、严格 Origin 与 redirect URI 校验。
- GitHub Client Secret 只保存在 Cloudflare encrypted secret。
- AI 和 GitHub 网络请求增加 timeout 与 AbortController。
- README 使用 worker 解析、HTML 净化和 CSP 约束；构建验证不允许 `unsafe-eval`。

### Known limitations / 已知限制

- v4 完整备份不包含 AI 任务、任务项、README 缓存和分类迁移快照。
- 完整备份导入和“清空全部数据”当前未清除 AI 任务、任务项与 README 缓存，下一批必须优先修复。
- 尚无 StarHub 云端账号、跨设备自动同步、团队协作或启用的 PWA。
- AI 准确度尚无 200–500 项公开可复现人工金标准，不承诺固定百分比。

## [1.0.0] - 2024-12-26

### Added

- GitHub OAuth 登录和 Stars 同步。
- 本地分类、搜索、筛选和 README 预览。
- IndexedDB 本地存储、深浅主题与中英文基础界面。
- OpenAI、Claude、DeepSeek、Qwen 和智谱初版 AI 分类入口。

> 1.0.0 的部分实现已在后续批次重构；当前行为请以最新文档为准。
