name: CI

on:
  push:
    branches:
      - main
      - 'agent/**'
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Lint, type-check, test, audit, and build
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type-check
        run: npm run type-check

      - name: Run unit tests
        run: npm run test:unit

      - name: Type-check Cloudflare Functions
        run: npm run cloudflare:type-check

      - name: Build application and documentation
        run: npm run pages:build

      - name: Verify static security policy
        run: npm run security:verify

      - name: Audit production dependencies
        run: npm run audit:production

      - name: Build Cloudflare Pages bundle
        run: npm run cloudflare:build
