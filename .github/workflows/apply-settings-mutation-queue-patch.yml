name: Apply settings mutation queue patch

on:
  push:
    branches:
      - agent/tag-relations-single-source
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: settings-mutation-queue-patch
  cancel-in-progress: true

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/tag-relations-single-source
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc

      - name: Apply Settings mutation queue patch
        run: node scripts/apply-settings-mutation-queue-patch.mjs

      - name: Commit generated Settings changes
        run: |
          if git diff --quiet -- src/pages/Settings/index.vue; then
            echo "No Settings changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/Settings/index.vue
          git commit -m "fix: serialize Settings data maintenance"
          git push origin HEAD:agent/tag-relations-single-source
